# 文案

- 按钮使用动作加对象，例如“保存内容”“新建项目”。不要用含义不明的“确定”覆盖所有动作。
- 默认、空结果、加载、失败、保存成功分别说明当前发生了什么。
- 错误写清影响和恢复路径，例如“名称不能为空；填写后重新保存”。保持用户已输入的内容。
- 界面示例里的英文工作台文案属于展示内容；组件标签和提示可由接入项目替换，不把语言算作新套系。
- 只有保存、上传或发布的实际结果返回后，才显示已完成。没有后端的演示必须注明本地范围。
