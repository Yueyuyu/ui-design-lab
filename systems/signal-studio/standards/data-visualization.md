# 数据与图表

- 数据、单位、周期和更新时间由调用方明确提供；没有更新时间就说明未记录。
- 零、负值、空集合、单点和非法值含义不同，不补出虚构的趋势。
- 柱图保留零轴与正负方向，读数同时包含类别和单位，键盘用户也能逐项读取。
- 表格的排序、筛选、选择与分页使用同一数据集；跨页选择不能悄悄丢失。
- 当前 DataTable 是客户端集合操作，不能把它描述为服务端分页；远程请求由接入项目实现。
