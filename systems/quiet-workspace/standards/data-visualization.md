# 数据图表规范

- 图表使用 `--qw-color-chart-1` 至 `--qw-color-chart-5`，同一图表由浅到深表达强度或时间推进。
- 红、橙、绿状态色不作为普通系列色，只有告警、执行中和成功语义才能使用。
- 图表必须同时提供标题、单位、数值标签或可访问名称，不能只靠颜色区分。
- 网格线使用 `--qw-color-chart-grid`，视觉权重低于数据。
- 默认不使用渐变、3D、面积阴影和持续动画。
- 加载、空数据和错误属于图表组件状态，必须保持绘图区高度，避免布局跳动。
