# 图标规范

- 统一使用 Phosphor Icons，不混用 emoji、文本符号或其他图标家族。
- 默认使用 `regular`；强调状态、12–14px 小图标可使用 `bold`，品牌或选中态才使用 `fill`。
- 紧凑控件 16px、舒适控件 18px、导航 20px、展示图标 24px。
- 图标与文字间距为 8px；纯图标按钮必须提供可访问名称。
- 图标只辅助表达，错误、成功和警告仍需配套中文文字。
- 同一操作在所有项目中使用同一图标，例如删除始终使用 `Trash`，关闭始终使用 `X`。
