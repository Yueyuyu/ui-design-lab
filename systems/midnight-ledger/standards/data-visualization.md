# Data Visualization

- 收益方向：正值 `--ml-positive`，负值 `--ml-negative`，基准线使用 `--ml-chart-grid`。
- 折线使用奶油色主线；填充只能是同色低透明度，不使用渐变。
- 图表必须显示周期、单位、最新值和文本摘要；悬停与键盘焦点共享数据点提示。
- 红绿之外必须同时显示正负号和数值。避免装饰性平滑、3D、光晕和无意义面积图。
