# UI Design Lab 设计验收

日期：2026-09-05。验收范围为用户授权的初步设计系统与可运行工作台示例。公共首页遵循统一展示反馈；新套系根据选定视觉稿建立独立视觉语言。

## 视觉来源与归一化

- Clearline Console：`references/clearline-console-source.png`，1487×1058 像素。
- Signal Studio：`references/signal-studio-source.png`，1487×1058 像素。
- 实现：本地 `#systems/clearline-console/overview`、`#systems/signal-studio/overview`，源稿中的项目目录/内容看板状态。
- 桌面实现 CSS 视口 1840×1380、device scale 1；完整截图约 1840px 宽，以 *-final-box.json 记录的实际工作台矩形裁切。工作台宽 1262 CSS px；澄明后台高约 672px，信号创作间高约 1061px。外层 Gallery 文档导航不属于源稿中的业务工作台，因此从对照中裁去。
- 源稿与实现均等比缩至 744px 宽，放入同一张 1488px 宽对照图。没有强行拉伸成同高；这是响应式容器中的视觉语言对照，不是 1:1 像素复刻的证明。
- 手机额外使用 390×844；首页另检查 1440、859px；封面固定 800×400，首页等比缩放显示。

证据目录：`C:/Users/Yueyu/.codex/visualizations/2026/09/05/01a06f53-bd76-7fb2-bf9f-109e8d422bfb/`。

完整对照：clearline-comparison-before.png / clearline-comparison-after.png / clearline-comparison-final.png；signal-comparison-before.png / signal-comparison-after.png / signal-comparison-final.png。

局部对照：clearline-detail-final.png（标题、Tabs、字段）、signal-detail-final.png（主封面文字、材质与裁切）。局部图用于确认具体表面，不拿局部裁切差异推断整个页面的高度。完整原始实现保留在 clearline-final.png、signal-final.png。

## 发现与修订历史

| 轮次 | 发现 | 修正与复核 |
|---|---|---|
| 1 | P1：Signal 主封面大字横向裁切，内容图失去留白 | 画布按选稿比例布局，主封面保留完整图片，重新捕获同场景；最终文字与边界完整 |
| 1 | P2：Clearline 详情关闭文字换行，右栏挤压表格 | 改 Phosphor X 固定按钮；以容器宽度控制详情布局；删除重复“查看”列，项目名直接进入详情 |
| 1 | P2：新套系导航像一组独立按钮，层级不符 | Clearline 改平直行导航与蓝色当前项；Signal 改水平导航/下划线，动作区置于顶部 |
| 1 | P2：Signal 标题、修订链接和创建动作与源稿层级漂移 | 恢复衬线大标题、修订文字链接和紫色主动作，去除导航样式对动作按钮的影响 |
| 2 | P2：手机 Popover 被祖先 transform 改变定位基准 | 改为原生 Popover 顶层；截图 mobile-popover.png 及点击命中、边界矩形验证通过 |
| 2 | P2：800×400 封面的图表标签/末尾按钮被截断 | 统一画布布局高度，缩短展示图表轨道与控件间距；四套 PNG 重拍，首页实际加载后检查 |
| 3 | 最终复核 | 同画面重新比较源稿与最终实现；以上 P1/P2 均已消除，手机主题与总览未出现页面级溢出 |

## 五个必查表面

| 表面 | 结论 |
|---|---|
| 字体与排版 | 公共首页使用中性无衬线；Clearline 使用清楚的企业后台层级；Signal 使用 Georgia 衬线标题与系统无衬线正文。源稿未指定可分发字体文件，不声称字形完全相同；标题/正文层级保留 |
| 间距与布局 | 企业后台维持侧栏、表格和详情；内容工作台维持横向导航、主封面加两个次封面、修订列表。窄容器下结构转为顺序阅读。首页封面/文字/元数据采用统一顺序与尺度 |
| 色彩与 Token | Clearline 蓝/白/浅灰、Signal 紫/暖纸/黑各自独立；图表与进度使用本套品牌色。用户主题只改预览局部，不覆盖官方 Token；构建执行 Token 双向校验 |
| 图片与资产 | Signal 使用专门生成的真实 PNG/WebP 素材，未用 CSS/SVG 图案代替；PNG 原件保留。首页封面是同一场景实际组件截图，第三方母版不再作为宣传封面 |
| 文案与内容 | 标题、状态、动作有对应本地交互；英文业务样例保留选稿语境，公共说明使用中文。模拟任务/上传明确说明边界，付费内容未伪装为已上架商品 |

## 有意保留的差异与 P3 后续打磨

- 生成稿中的全部企业菜单、成员管理、时间线、频道等不属于本轮业务后端范围；示例只保留能够执行的相关导航。它们不是套系“缺失 API 已支持”的声明。
- 业务数据日期、负责人及字段是可替换演示数据；界面保留中文可访问名称。实际可编辑控件与源稿静态字段会有局部尺寸差异。
- Georgia 与源稿衬线的具体字形、生成素材的纹理与曲线存在细微差异，作为 P3 视觉打磨保留；不影响独立视觉语言与交互验收。新套系因此继续保持 experimental。
- 本轮未覆盖 Firefox/Safari、屏幕阅读器和系统缩放。不能将这里的通过解释为全浏览器或完整无障碍认证。

## 交互与运行核对

四套保存、任务、选择、菜单/抽屉、通知和主题；比较快照与分享恢复；研究版本；报表日期与导出；新系统创建/编辑；仓库外四套运行均有浏览器检查。最终日志只有早期开发热更新错误，没有最终复核阶段新增错误，详情见 docs/ACCEPTANCE.md。

## 实施核对

- [x] 对照源稿与实际浏览器截图，检查完整视图及关键局部。
- [x] 修正裁切、导航、详情关闭、动作层级与手机浮层问题。
- [x] 重拍修订状态，完成字体、间距、色彩、图像和内容检查。
- [x] 固定同场景封面生成入口，保留原稿与权属说明。
- [x] 区分初步视觉验收、浏览器覆盖限制和实际业务上线。

final result: passed
