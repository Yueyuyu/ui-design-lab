import{resolve}from"node:path";import{starterFiles}from"./lib/starter-files.mjs";const args=process.argv.slice(2);const option=name=>args.find(a=>a.startsWith("--"+name+"="))?.split("=").slice(1).join("=");const target=option("target");if(!target)throw Error("使用 --target=新目录，可选 --suite=套系ID");console.log("已生成 "+(await starterFiles(resolve(target),option("suite"))).join("、")+"："+resolve(target));
