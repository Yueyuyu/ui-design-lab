# 首页场景封面 · 2026-09-22

八张 PNG 均为用户在本轮对话中选择的 Image Gen 原始结果，复制时逐一比对 SHA-256，未裁切或改画。完整映射、生成结果 ID 与校验值见 `selections.json`；运行时以各套 `suite.json` 中的 `selection.scenePreview` 为准。

首页使用 `references/homepage-gallery-selected-20260922.png` 的画廊结构，统一封面外部画幅，完整缩放选中场景。所有注册套系沿现有目录顺序完整展示一次，不将后四套缩成第二份文字目录。

这些图片是设计语言的应用概念，不是现有可运行页面、已接入服务或真实项目采用的证据。图片内的文字、数据与状态为示例。套系能力以组件、类型、交互规范与文档为准；原组件封面和 `#/cover/<suite-id>` 导出入口保持原用途。

脉点选中图使用静态品牌图标与既有黑色额度环、展开面板，不包含第三方机器人素材。场景图片属于 Gallery 展示资源，不随套系组件包分发。
